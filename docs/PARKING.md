# Open Science Pillars: Parking Lot

The holding pen required by harness rule 11: SPECIFICATION.md is frozen until the Session 5 integration test, so ideas and gaps land here with a one-line rationale instead of becoming spec churn. Triage into SPEC v0.6 happens after Session 10 unless an item's triage target says otherwise. This file moves into `marketplace/docs/` at Session 1.

Format: item | why it matters | triage target.

| # | Item | Why it matters | Triage target |
|---|---|---|---|
| 1 | Define launch success criteria beyond acceptance: for example, at least one external install with an issue filed, at least one non-author completing Tutorial 2 unaided, the ablation delta published with intervals, one steward conversation opened with PO.DAAC. | Acceptance criteria prove we built it right; evals prove it behaves; neither tells us whether anyone was helped. Announcing against pre-stated criteria keeps the launch honest. | Session 13, written into the announcement plan. |
| 2 | Pre-register Phase-2 go and stop conditions: what result pattern would argue against continuing (for example, the bundle ablation shows no behavioral effect, or zero external engagement 30 days post-launch), and what pattern funds doubling down. | Pre-registration is the antidote to sunk-cost drift; deciding the exit test before the result exists is the same epistemics we demand of the science. | After Session 10, alongside S18-19 planning. |
| 3 | Restore the non-author acceptance criterion to SPEC §9 ("a non-author scientist completes the end-to-end workflow"), which existed in v0.1 and was dropped during the v0.2-v0.3 consolidation. Operational fix already applied in Guide Session 14. | External validation before announcement is the difference between "works for the author" and "works." | SPEC v0.6. |
| 4 | Confirm Claude usage-limit headroom for a ~35-hour agentic week across three surfaces, and schedule the heavy sessions (5, 6, 10) accordingly. | A limit collision mid-Session-6 is a bad way to discover plan constraints. | Day 1 morning, before Session 1. |
| 5 | Align SPEC §0.1 packaging matrix and §2.3 install text for Claude Science: observed in the Session 0b smoke test (2026-07-04) that Science installs plugins from an unlisted marketplace exactly like Cowork; the spec describes workspace skill import plus connector config instead. | Install docs, READMEs, and Tutorial 1 would otherwise teach a clunkier path than the product offers, and the parity story is stronger than the spec claims; surface-testing-guide.md already records observed behavior. | SPEC v0.6. |
| 6 | Update the SPEC §2.2 marketplace.json verbatim block to the schema the Claude Code CLI actually validates: a required `owner` object, and plugin entries using `source: {source: github, repo: owner/repo}` instead of `repository`. Both deltas discovered and fixed during the Session 1 install test; the committed marketplace.json is the working reference. | The spec's copy-paste block fails `claude plugin marketplace add` as written; anyone scaffolding from the spec hits the same two errors. | SPEC v0.6. |
| 7 | Replace "verified with /doctor" (SPEC §0.3, and the same wording in the osp-close gate and Session 2 guide text) with the /skills panel: /doctor in CLI 2.1.201 is environment diagnostics only, while /skills lists every loaded skill with per-description token cost and truncation state. | The description-budget verification step points at a check that no longer exists; /skills is the actual instrument and shows per-skill evidence the old check never did. | SPEC v0.6; harness skill wording is Paul's call anytime. |
| 8 | Add a fill-value-detection eval case to core/evals/ and point common-fill-values.md's eval_case at it. The concept is severity high (silently wrong statistics) but core's frozen Phase-1 case set (area-weighting, uncertainty-statement, trend-method) has no fill-value case, so harness rule 9 stands unmet as a documented linter 🟡. | Rule 9 exists precisely for traps like this one; the bad-data fixture already provides the test vehicle, so the case is cheap to author once the freeze lifts. | SPEC v0.6, with the Session 10 grading pass as the earliest alternative if the freeze is amended. |
| 9 | Gotcha candidate for the ocean bundle: V4R4 vs V4R4B release mixing. The Session 6 CMR sweep (2026-07-04) found B-release collections for SSH and OBP only (`..._V4R4B`); mixing releases within one analysis, or comparing against literature quietly pinned to the other release, shifts SSH/OBP results. The Phase-1 gotcha set is frozen at five, so this rides in the variable catalog's Variants section meanwhile. | Exactly the silently-wrong-results shape the knowledge layer exists for, discovered by our own audit; approved for parking by Paul 2026-07-04. | SPEC v0.6 ocean bundle; eval case alongside per rule 9. |
| 10 | Gotcha candidate: ecco_access (0.3.1) mishandles static collections. Given ECCO_L4_GEOMETRY_LLC0090GRID_V4R4 with no date range it synthesized a nonexistent dated granule filename (archive 404) and estimated 75.8 GB for a one-granule collection. Recorded in the variable catalog's access section (Session 6); workaround is earthaccess for statics. Worth an upstream issue on the ecco_access tracker too. | Every new ECCO user loads geometry first, so this is the first failure they hit; the 75.8 GB estimate could also scare users into aborting legitimate setups. | SPEC v0.6 ocean bundle (or upstream fix, then just the catalog note); evidence: reproduced 2026-07-04. |
| 11 | Gotcha candidate: MHT basin scope. ecco_v4_py's section machinery given only a latitude computes the full circle; a full-circle number quoted against RAPID (Atlantic) is plausible-looking and wrong by ~0.4 PW. Discovered by a test agent in Session 9, verified by basin decomposition; recorded in the recipe, the transport skill, and the golden meanwhile. Also note ecco_v4_py's basin masks need its binary data files installed (env quirk). | The exact silently-wrong-comparison shape the knowledge layer exists for; our own recipe shipped with the wrong scope for a session. | SPEC v0.6 ocean bundle; eval case alongside per rule 9. |
| 12 | Author salt and volume budget recipes for the ocean bundle (analogous to ecco-heat-budget: inputs, measured absolute residual expectations, tutorial provenance). The Session 9 close lint caught budget-formulation.md claiming they existed; the claim is corrected, the recipes are the real fix. | Budget skills promise salt and volume support; without recipes their tolerances are unpinned. | SPEC v0.6 ocean bundle. |

## Log
- 2026-07-03: file created; items 1-4 seeded during the final pre-build review round.
- 2026-07-04: item 5 added during the Session 0b assumption smoke test (Science install mechanism).
- 2026-07-04: item 4 resolved before Session 1: usage-limit headroom confirmed by Paul.
- 2026-07-04: item 6 added during the Session 1 marketplace install test (CLI schema deltas).
- 2026-07-04: item 11 added during the Session 9 spot tests (MHT basin scope).
- 2026-07-04: item 12 added at the Session 9 close lint (missing salt/volume recipes).
- 2026-07-04: item 7 added at Session 2 close (/doctor superseded by /skills for the budget check).
